<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Return to plate{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Remove</button>
</li>